# Story 2.42 — A pontuação vira SOB DEMANDA: o trigger chama a IA na hora, e o cron sai de cena

**Status:** 🔍 InReview — @po GO 10/10 (17/08) · implementada · gate do @qa: **CONCERNS**
**Epic:** 2 — Qualificação automática de leads (CRM Acreditando)
**Criada em:** 2026-08-17
**Autor:** @sm (River)
**Severidade:** 🟠 Alta — remove o último polling do caminho da IA
**Estimativa:** **M**
**Depende de:** **story 2.41** (fila `ai_pending_lead_scores` + trigger de entrada) — não deployada ainda; as duas sobem juntas

---

## História

**Como** Filipe,
**quero** que a análise da IA dispare **no momento em que o card entra em `Qualificado`** — por evento, não por varredura —,
**para que** cada lead consuma **uma** chamada no instante certo, sem nenhuma rodada vazia rodando de 5 em 5 minutos.

---

## O pedido, literal (Filipe, 17/08)

> *"Para a análise da IA para verificar a qualificação do lead quando entrar na coluna
> Qualificação, eu **não quero que seja com cron**. Quero que ao entrar lá dispare um webhook
> ou algo tipo para mandar os dados para a IA analisar. **Eu não quero cron, quero sob demanda.**"*

### Por que o pedido está certo

A 2.41 consertou o **sangramento** (fila com contador de tentativas), mas manteve o **polling**:
12 rodadas/hora, 24h/dia, para uma fila que só enche em horário comercial. Já estava registrado
no diário de 17/08 como desperdício por desenho — *"2/3 das rodadas rodam quando ninguém arrasta
card nenhum"*.

**Sob demanda ganha duas vezes:** custo proporcional a lead real (zero rodada vazia) **e** nota
em segundos em vez de esperar até 5 minutos.

---

## 🧭 Decisão de desenho: `pg_net` no trigger — o padrão já existe nesta casa

`pg_net 0.20.4` está **instalado em produção** e o repo **já dispara HTTP de dentro de trigger**:
`notify_deal_stage_changed` (em `20251201000000_schema_init.sql`, linha ~2187) faz
`net.http_post` e persiste o `request_id`. **É ADAPT, não CREATE** (IDS).

```
Fernanda arrasta   ─┐
IA qualifica        ─┤
webhook / API / MCP ─┼─→ UPDATE deals.stage_id ─→ TRIGGER ─┬─→ INSERT na fila (livro-caixa)
tela / import       ─┘                                      └─→ net.http_post → /api/ai/pontuar-lead
```

O trigger continua sendo o **ponto único** — é o que cobre as 8 portas de entrada (razão inteira
da 2.41). O que muda é que ele deixa de só enfileirar e passa a **chamar**.

### A fila NÃO some — ela troca de papel

| | Story 2.41 | Story 2.42 |
|---|---|---|
| Fila `ai_pending_lead_scores` | **motor** (o cron drenava) | **livro-caixa** (`attempts`, `last_error`, `request_id`) |
| Quem dispara o trabalho | cron, 12×/hora | **o trigger, na entrada** |
| Cron `pontuar-leads-qualificados` | ativo | ❌ **`unschedule`** |
| Rede de segurança | — | 1× por dia, só sobre quem ficou sem nota |

📌 **Tirar a fila junto seria trocar um defeito por outro.** Sem ela não há `attempts` (o laço
infinito volta na primeira falha que retentar), não há `last_error`, e não há como saber que um
disparo se perdeu.

---

## ⚠️ O que este desenho PIORA, dito na cara (e como é coberto)

`pg_net` é **fire-and-forget**. Se o POST falhar — app em cold start, deploy no meio, 500,
timeout —, **nada tenta de novo**. Trocar polling por evento troca *"gasta demais"* por
*"pode perder em silêncio"*.

🪤 **Perder em silêncio é a classe de defeito que mordeu este repo três vezes só em agosto**
(alarme do Lead Ads · Mission Control inativo · log de tokens cego). O padrão registrado é
sempre o mesmo: *o caminho de erro existe e ninguém escuta.*

**Decisão do Filipe (17/08): rede de segurança 1× por dia.** Um job diário varre quem ficou sem
nota e redispara. Continua sendo o evento que faz o trabalho; o job só **cata o que caiu**, e
age sobre 0 cards no dia em que nada falhar.

> 📌 De **288 rodadas/dia** para **1** — e essa 1 existe só para que uma falha não vire silêncio.

---

## Critérios de Aceite

### AC1 — O disparo é por EVENTO, e o polling morre
- [ ] O trigger de entrada chama `net.http_post` para `POST /api/ai/pontuar-lead`.
- [ ] `cron.unschedule('pontuar-leads-qualificados')` — o job de 12×/hora **deixa de existir**.
- [ ] Read-back em `cron.job` provando que ele sumiu e que só o diário ficou.

### AC2 — O disparo não pode derrubar o arrasto da Fernanda
- [ ] A chamada HTTP fica em bloco `EXCEPTION WHEN OTHERS` — falha de rede **não** pode
      abortar o `UPDATE` do card. Arrastar tem de funcionar mesmo com o app fora do ar.
- [ ] O disparo é **assíncrono** (`pg_net` envia depois do commit): a tela não espera a IA.
- [ ] Falha no disparo grava motivo em `last_error`, e o item continua `pending` — a rede de
      segurança do dia seguinte o encontra.

### AC3 — `timeout_milliseconds` explícito (o default quebraria toda pontuação)
- [ ] `net.http_post` recebe `timeout_milliseconds := 30000`.
- [ ] ⚠️ **O default do `pg_net` é 5.000 ms e a pontuação leva 6,0 s em média (p99 16 s)** —
      medido no console do Google em 16/08. Com o default, o `pg_net` cortaria a conexão no meio
      de **toda** chamada.

### AC4 — Uma lógica só, compartilhada
- [ ] A pontuação de **um** item vive num módulo único, usado pelos **dois** caminhos
      (o endpoint sob demanda e a rede de segurança).
- [ ] ⚠️ Duas implementações do mesmo trabalho é exatamente o defeito que a story 2.29 passou o
      dia consertando (três traduções manuais do mesmo registro).

### AC5 — Rastro do disparo
- [ ] `ai_pending_lead_scores` ganha `request_id` (o id do `pg_net`) e `dispatched_at`.
- [ ] Permite responder *"o disparo saiu?"* sem adivinhação — cruzável com `net._http_response`.

### AC6 — Idempotência preservada (herdada da 2.41)
- [ ] Card já pontuado não redispara · nota manual intocável · um item `pending` por deal.
- [ ] Disparo duplicado (evento + rede de segurança na mesma janela) **não** gera duas chamadas
      de IA: o lock otimista (`status='pending'` → `processing`) protege.
- [ ] O teto de 3 tentativas continua valendo, agora contado entre os dois caminhos.

### AC7 — A rede de segurança é mínima e visível
- [ ] Um job diário (fora do horário de pico) que redispara **apenas** itens `pending` com
      `attempts < 3`. Em dia sem falha, processa **0**.
- [ ] O nome do job **não mente sobre a cadência** (pendência nº 41 do CRM:
      `stage-evaluations-1min` roda `*/5`).

### AC8 — Segurança
- [ ] Reusa o segredo `cron_secret_stage_eval` do `vault` — **nenhum segredo novo, nenhum
      segredo pelo chat**.
- [ ] O endpoint novo exige o mesmo `Bearer` — não fica aberto.
- [ ] Trigger continua `SECURITY DEFINER` com `search_path` fixo.

### AC9 — Card que NASCE em `Qualificado` também pontua
- [ ] O trigger de `INSERT` permanece (decisão do Filipe, 17/08): importação, API pública e
      integração entram já qualificadas e precisam de nota.
- [ ] ⚠️ **Risco aceito:** importação em massa vira rajada de disparos. Mitigado pelo teto de
      3 tentativas e por o custo ser 1 chamada por lead — mas **registrado**, não escondido.

---

## 🚫 Fora de escopo

- **Botão "Pontuar agora" na tela** — o Filipe escolheu a rede diária em vez do manual. Fica
  como melhoria possível, não como escopo.
- Instrumentar os 18 pontos sem log · corrigir o `provider-failover` · repontuar quem já tem nota.
- **Reaper de itens presos em `processing`** (ressalva 2 do gate da 2.41) — dívida de classe,
  vale também para o cron irmão. Story separada.
- Criar a chave nova e as travas no console do Google — ação do Filipe, fora do código.

---

## Riscos

| # | Risco | Mitigação |
|---|---|---|
| 1 | Disparo se perde e o lead fica sem nota | Rede de segurança diária + `last_error` + `v_leads_a_pontuar` como painel |
| 2 | `pg_net` corta a conexão antes de a IA responder | `timeout_milliseconds := 30000` (AC3) |
| 3 | Falha de rede aborta o arrasto do card | `EXCEPTION WHEN OTHERS` no bloco do disparo (AC2) |
| 4 | Evento + rede de segurança pontuam o mesmo card duas vezes | Lock otimista `pending → processing` (AC6) |
| 5 | Importação em massa vira rajada | Risco aceito e registrado (AC9); custo é 1 chamada/lead |
| 6 | URL do app fixa na migration | Segue a convenção já existente (`20260813011000`); trocar de domínio exige nova migration — **registrado como dívida** |

---

## Definition of Done

- [~] AC1–AC9 provados — o que é banco **só fecha no read-back pós-migration** (ver gate)
- [x] `npm run precheck` verde (lint + typecheck + 790 testes + build) — rota `ƒ /api/ai/pontuar-lead` registrada
- [x] Migration idempotente, **sem `DROP`** de tabela/view
- [x] Gate do @qa registrado — ver **QA Results** abaixo
- [ ] ⚠️ **Deploy e migration pelo Filipe/@devops.** Ordem obrigatória, herdada do gate da 2.41:
      **migration → deploy → chave**

---

## QA Results — gate de 2026-08-17 (@qa Quinn)

**Veredito: ⚠️ CONCERNS** — aprovada, e **com uma dívida que este desenho torna mais urgente
do que era**.

Commit revisado: `3ec3425` · branch `story/2.41-pontuacao-na-entrada-da-coluna`

### Os 7 checks

| # | Check | Resultado |
|---|---|---|
| 1 | Code review | ✅ adapta o padrão existente (`notify_deal_stage_changed`); uma lógica só para os dois caminhos |
| 2 | Testes unitários | ✅ 790 passando; as regras puras seguem cobertas por `filaDePontuacao.test.ts` |
| 3 | Critérios de aceite | ⚠️ AC1/AC2/AC3/AC5/AC7 dependem de **read-back** — nenhum deles é provável em teste local |
| 4 | Regressões | ✅ a fila, o teto de 3, a dispensa e o read-back do `UPDATE` sobreviveram intactos |
| 5 | Performance | ✅ `pg_net` é pós-commit ⇒ o arrasto não espera a IA; orçamento de relógio na rede de segurança |
| 6 | Segurança | ✅ segredo do `vault` (nenhum novo, nenhum pelo chat) · endpoint exige `Bearer` · `SECURITY DEFINER` com `search_path` fixo |
| 7 | Documentação | ✅ a migration explica o custo, o timeout e o porquê da rede |

### 🔴 A ressalva que MUDOU de tamanho — o reaper virou urgente

O gate da 2.41 registrou que um item pode ficar preso em `processing` se a execução morrer
entre o lock e o registro da falha, e classificou como *"não bloqueia"*. **Com a 2.42 isso
piora, e é preciso dizer com todas as letras:**

- Antes, um item preso em `processing` era um card sem nota que **ninguém recuperava** — mas o
  cron rodava 12×/hora, então a janela de morte era pequena e frequente o bastante para chamar
  atenção.
- Agora o caminho normal é o endpoint sob demanda, com `maxDuration = 60`. **Se a função for
  cortada no segundo 60 no meio de uma pontuação, o item fica `processing`.**
- E a rede de segurança filtra `status = 'pending'` ⇒ **ela não enxerga `processing`.**
  O card fica sem nota **para sempre**, e o único lugar onde aparece é `v_leads_a_pontuar`.

📌 O orçamento de relógio (`ORCAMENTO_MS`) que entrou nesta story protege **só o caminho em
lote**. O caminho sob demanda não tem como se proteger: quem corta é a plataforma.

**Recomendação — story 2.43, e agora com prioridade real:** devolver a `pending` todo item em
`processing` há mais de N minutos, e contar a tentativa. Vale também para o cron irmão
`stage-evaluations`, que tem a mesma lacuna. **Enquanto ela não subir, `v_leads_a_pontuar` é a
única testemunha** — e este repo já aprendeu em 16/08 o que vale uma testemunha que ninguém lê.

### 🟡 Ressalva 2 — o `vault` é o ponto único de falha do disparo

O trigger lê `vault.decrypted_secrets` como `SECURITY DEFINER` (dono: `postgres`), o que
**deve** funcionar mesmo quando quem arrasta o card é a Fernanda autenticada. Mas isso é
inferência sobre permissão, não medição.

Se falhar, o desenho **degrada com elegância** — o `EXCEPTION` grava `last_error`, o item fica
`pending` e a rede diária o recupera. Ou seja: **o sintoma não seria "quebrou", seria "a nota
demora até um dia"**, que é justamente o tipo de falha que passa despercebida.

⚠️ **É o item nº 1 do read-back:** arrastar um card e confirmar que `dispatched_at` e
`request_id` foram preenchidos. `dispatched_at IS NULL` com `last_error` preenchido = o disparo
não saiu.

### 🟡 Ressalva 3 — timeout do `pg_net` ≠ pontuação falhada

Se a pontuação passar de 30s, o `pg_net` registra timeout em `net._http_response` — **mas a
função da Vercel continua rodando** e provavelmente grava a nota normalmente. Ao investigar,
**não** ler timeout do `pg_net` como "não pontuou": a fonte da verdade é `status` na fila e
`pontuada_pela_ia_em` no deal.

### Read-back obrigatório (o que só fecha depois da migration)

1. `select jobname, schedule from cron.job` → `pontuar-leads-qualificados` **sumiu**,
   `pontuar-leads-rede-de-seguranca` presente com `17 6 * * *`
2. Arrastar **um** card para `Qualificado` → conferir na fila: uma linha, `dispatched_at`
   e `request_id` preenchidos, e em seguida `status = 'completed'` com a nota no card
3. Deixar a **IA** qualificar outro (`moveOnQualified`) → mesma conferência (fecha o AC1 da 2.41,
   que estava provado só por desenho)
4. `select count(*) from ai_pending_lead_scores where status = 'processing'` → deve ser **0**
   em regime; qualquer número aqui é a dívida da ressalva 1 se materializando

### Change Log

| Data | Agente | Mudança |
|---|---|---|
| 2026-08-17 | @sm | Story criada (Draft) |
| 2026-08-17 | @po | Validação 10/10 → **GO**; Draft → Ready |
| 2026-08-17 | @dev | Implementada — commit `3ec3425`, precheck verde |
| 2026-08-17 | @qa | Gate **CONCERNS**; reaper elevado a urgente (proposta story 2.43) |
