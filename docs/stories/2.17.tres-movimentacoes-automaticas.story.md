# Story 2.17 — As três movimentações automáticas: o card anda sozinho até onde a IA sabe

**Status:** 🟢 **Ready for Dev (rev. 3)** — escopo reduzido a **T2 + T3** pelo @po em 11/08. O **T1 saiu** e virou número de painel na story **2.19** (decisão do Filipe: caminho **b**).
**Epic:** 2 — Qualificação automática de leads (CRM Acreditando)
**Criada em:** 2026-08-07
**Atualizada em:** 2026-08-11 — @sm, incorporando as respostas da Fernanda
**Autor:** @sm (River)
**Severidade:** 🔴 **Alta** — é o que faz o board dizer alguma coisa sozinho; sem isso a Fernanda continua classificando 846 leads/mês na mão
**Prazo:** ⏰ **2026-08-13**
**Estimativa:** **G** (grande) — mexe em caminho de escrita disparado por webhook, com dado real chegando

---

## História

**Como** Fernanda,
**quero** que o card se coloque sozinho na coluna certa conforme o lead responde (ou não),
**para que** eu abra o board de manhã e já saiba **quem vale trabalhar agora**, sem ler conversa por conversa.

---

## Contexto

Compromisso do Filipe na reunião de 07/08, dito nestas palavras:

> *"Vai ter as duas movimentações que vão ser sozinhas, por enquanto: o lead novo que não responder nada; o lead que responder alguma coisa mas não foi qualificado; e qualificado."*

E a dor que isso ataca, dita por ela em 16/07 e repetida em 07/08:

> *"Eu falei com ele, a IA entrou muito pouco… ele não interagiu, eu que interagi com ele direto. **Então eu tenho que fazer essa classificação manual.**"*

### O que já existe (não inventar de novo)

| Peça | Onde | Estado |
|---|---|---|
| Criação do deal ao entrar lead | `lead_routing_rules` (`board_id` + `stage_id`) | ✅ no ar |
| Mover para "Em qualificação" na transferência para humano | `lead_routing_rules.transfer_stage_id` (story 2.5) | ✅ no ar desde 03/08 — **destino por UUID**, e o card **nunca anda para trás** |
| Extração de campos por IA | `lib/ai/extraction/customFields.service.ts` | ✅ no ar desde 28/07 |
| Regra viva hoje | **1** (canal `gptmaker`) — as outras 3 eram órfãs e foram apagadas em 07/08 | ✅ |

⇒ **Esta story estende um mecanismo existente; não cria um motor novo.** O padrão da 2.5 (destino por UUID configurável + monotonicidade) é lei aqui.

---

## As três transições

| # | Nome de trabalho | Condição | Gatilho |
|---|---|---|---|
| **T1** | *Sem resposta* | recebeu mensagem de saída e **nenhuma `inbound`** desde então, por **N horas** | tempo (job), não evento |
| **T2** | *Respondeu, não qualificou* | existe ≥1 `inbound` **e** os campos de qualificação seguem vazios | mensagem inbound |
| **T3** | *Qualificado* | **`onde mora` E `tipo de lesão` preenchidos** — os dois, definidos por ela em 11/08 | fim da extração |

### 📌 As definições da operação — respondidas pela Fernanda em 2026-08-11

Respostas literais, por WhatsApp, à pergunta enviada pelo Filipe:

> *"1. 24 horas"*
> *"2. Onde mora e o tipo de lesão"*

| Definição | Valor | Onde entra |
|---|---|---|
| **Sem resposta** | **24 horas** sem `inbound` | `N` do **AC4** — default 24, configurável |
| **Qualificado** | **`onde mora`** **E** **`tipo de lesão`** — os dois preenchidos | condição do **T3** |

🔑 **A resposta dela é mais estreita do que a extração produz.** A IA extrai **cinco** campos
(tipo de lesão · tempo · para quem é · região · já fez reabilitação). Ela usa **dois**.
⇒ *"qualificado"* **não** é "extração completa"; é **exatamente estes dois**. Exigir os cinco
deixaria de fora lead que ela considera pronto — e é a diferença entre a coluna encher e ficar vazia.

⚠️ **As `key` exatas dos dois campos saem do AC0** (`custom_field_definitions`), **não deste
documento** — os rótulos acima são as palavras dela, não identificadores do banco.

🪤 **T1 é de natureza diferente das outras duas: é ausência de evento.** Webhook não dispara quando nada acontece. Precisa de um job periódico (`pg_cron` já está no repo) — e é a mesma família do alarme que faltou no Lead Ads (*"o alarme cobre lead que entra e falha, não lead que nunca entra"*).

---

## Critérios de aceite

### AC0 — Medir a realidade antes de escrever regra *(gate)*

Contra a produção, antes de codar:

```
estágios reais do board Acreditando (id, name, "order")   ← nomes têm espaço no fim (armadilha da 2.5)
custom_field_definitions da org (key, label, type, options)
distribuição atual dos deals por stage_id
quantos deals têm ≥1 mensagem inbound
quantos deals têm todos os campos de qualificação preenchidos
```

**Se a distribuição mostrar que T2 e T3 já cobrem quase tudo, o valor da story cai** e ela volta ao @po. Números na story.

### AC1 — Destino configurável, nunca hard-coded

As três transições apontam para estágios por **UUID**, em configuração (extensão de `lead_routing_rules` ou tabela irmã) — **nunca por nome**. Motivo registrado na 2.5: *o estágio de destino tem espaço no fim do nome*, e casar por texto falharia em silêncio.

### AC2 — O card nunca anda para trás

Movimentação automática só avança na ordem do funil (`board_stages."order"`). Um lead qualificado que mande mensagem nova **não volta** para "sem resposta". Mesma regra da 2.5.

### AC3 — Movimento manual da Fernanda tem a palavra final

Se ela moveu o card na mão, a automação **não desfaz**. Ela é a única vendedora e conhece o caso; a máquina não sobrescreve decisão humana — mesmo princípio já escrito na extração (*"nunca sobrescreve o que a pessoa digitou"*).

### AC4 — T1 roda por tempo, com janela definida pela operação

Job periódico marca *sem resposta* após **N horas sem inbound** — `N` é **parâmetro configurável**, não constante no código. ✅ **`N` = 24 horas** (Fernanda, 11/08). O valor vive em configuração; **24 é o default, não uma constante literal** — ela mudou de ferramenta duas vezes em um mês e pode querer afinar isso depois de ver a coluna encher.

### AC4b — "Qualificado" são exatamente dois campos, e isso é verificado

O T3 dispara quando **`onde mora` E `tipo de lesão`** estão preenchidos. A regra **não** exige os
outros três campos que a extração produz. As `key` reais vêm do **AC0**; se algum dos dois **não
existir** em `custom_field_definitions`, a story **para e volta ao @po** — não se inventa campo.

### AC5 — Toda movimentação automática é auditável

Cada movimento grava atividade/timeline dizendo **que foi a automação** e **por quê** (qual regra, qual condição). A Fernanda precisa distinguir *"eu movi"* de *"o sistema moveu"* — ela já pediu isso implicitamente ao estranhar a IA movendo cards em nome dela.

### AC6 — Escrita conferida

Todo `UPDATE` de estágio usa `.select()` / `count: 'exact'` e trata **0 linhas como erro**. É a armadilha da 2.5 e da 2.15 — *o PostgREST devolve sucesso mesmo quando nada foi atualizado*.

### AC7 — Testes que falham sem o conserto

Cobrir: (a) lead sem inbound vai para T1 depois de N horas; (b) lead com inbound e campos vazios vai para T2; (c) lead com campos preenchidos vai para T3; (d) card movido na mão **não** é mexido; (e) card à frente no funil **não** retrocede. Revertida a implementação, os testes falham.

### AC8 — Provado em uso real

Um lead **real** entra e percorre pelo menos uma transição sozinho, com a data/hora registrada. Sem isso o status é *"aplicado, não provado"* (Rule 7 do Meta Ads, valendo dentro do código).

### AC9 — Gates do repo

lint 0 · typecheck 0 · suíte sem regressão · build ok.

---

## Fora de escopo

| Item | Por quê |
|---|---|
| **Estrelas 1–5** | Story **2.18** — é classificação, não movimentação |
| Mover para "ganho"/"perdido" automaticamente | Desfecho comercial é dela. Automatizar isso corromperia o número que ela apresenta |
| Fase *"contato recorrente sem prosseguimento"* | Pedida por ela em 07/08, virou **tag** primeiro. Vira coluna quando a operação decidir — story própria |
| Reescrever o motor de roteamento | Estende o que existe |

---

## Riscos e armadilhas

1. 🪤 **Nome de estágio com espaço no fim.** Já custou uma story. **Só UUID.**
2. 🪤 **T1 é ausência de evento** — nenhum webhook avisa. Sem job, a coluna fica sempre vazia e ninguém percebe: parece que "não tem lead sem resposta".
3. ⚠️ **Retroatividade.** Ao subir, a regra vai encontrar centenas de cards antigos elegíveis. **Decidir se roda no histórico ou só daqui pra frente** — uma varredura retroativa reorganiza o board inteiro de uma vez, na semana da apresentação.
4. ⚠️ **Interação com a story 2.16.** Se julho sair por soft delete ou mudança de board, a varredura precisa respeitar o recorte — senão mexe em card que a Fernanda nem vê.
5. ⚠️ **Concorrência.** Job periódico + webhook podem tocar o mesmo deal ao mesmo tempo. A corrida na criação de contato (story 2.6) já mostrou que este repo tem esse tipo de defeito **latente e invisível** — usar advisory lock ou condição idempotente.
6. ⚖️ **T3 lê resultado de extração de dado de saúde.** Não acrescenta acesso, mas **aumenta o uso** do dado estruturado. O gate de LGPD segue aberto desde 21/07.

---

## Perguntas em aberto

- [x] ✅ **Quantas horas para virar "sem resposta"?** → **24 horas** (Fernanda, 11/08)
- [x] ✅ **Quais campos definem "qualificado"?** → **onde mora + tipo de lesão** (Fernanda, 11/08)
- [ ] 🔴 **Rodar no histórico ou só daqui pra frente?** — **decisão do Filipe, ainda aberta.**
      Subiu de 🟡 para 🔴 por causa da data: a apresentação é **14/08, para diretoria + MKT**
      (confirmado por ela em 11/08). Uma varredura retroativa **reorganiza o board inteiro** na
      semana da apresentação — e ela **acabou de migrar** para o CRM. Ver *Riscos*, item 3.

## 📊 AC0 — medição contra produção (@dev, 2026-08-11)

Executado com `scripts/db/sql-ro.mjs` (somente leitura). Board **Acreditando**, `deleted_at is null`.

### 🛑 ACHADO QUE PARA A STORY: **o destino do T1 não existe no board**

Os 12 estágios reais, na ordem:

| `order` | Estágio | Deals |
|---|---|---|
| 0 | `Lead novo` | 141 |
| 1 | `Contato Realizado` | 51 |
| 2 | `Em qualificação ` ⚠️ *(espaço no FIM)* | 50 |
| 3 | `Qualificado` | 10 |
| 4 | `Apresentação enviada` | 14 |
| 5 | `Aguardando retorno` | 10 |
| 6 | `Avaliação agendada` | 4 |
| 7 | `Avaliação realizada` | 0 |
| 8 | ` Proposta enviada` ⚠️ *(espaço no INÍCIO)* | 0 |
| 9 | `Em negociação` | 0 |
| 10 | `Ganho` | 3 |
| 11 | `Perdido` | 16 |

**Não há nenhuma coluna de "sem resposta".** O T2 tem casa (`Em qualificação `, order 2) e o T3
tem casa (`Qualificado`, order 3). **O T1 não tem.**

E o candidato mais próximo pelo nome — `Aguardando retorno` — **está em `order` 5, depois de
`Qualificado`**. Usá-lo criaria uma contradição direta com o **AC2** (*o card nunca anda para trás*):
um lead que não respondeu em 24h seria empurrado **à frente** de "Qualificado" e, quando finalmente
respondesse e qualificasse, **não poderia mais voltar** — ficaria preso num estágio de fundo de funil
para sempre. ⇒ **A regra que existe para organizar a fila dela produziria uma fila mentirosa.**

➡️ **Volta ao @po.** Três caminhos, e a escolha é da operação:
**(a)** criar uma coluna nova antes de `Qualificado` · **(b)** T1 vira **tag**, não movimentação
*(há tabela `tags`, e ela já usa esse recurso — a fase "contato recorrente" virou tag em 07/08)* ·
**(c)** T1 sai desta story.

### ✏️ Correção de um erro meu na revalidação do @po

Eu escrevi que exigir os 5 campos deixaria a coluna *"quase vazia"*. **Medido: é falso.**

| Critério | Deals |
|---|---|
| `ondeReside` preenchido | 69 |
| `tipoDeLesao` preenchido | 73 |
| **Os dois (regra dela)** | **63** |
| **Todos os 5 campos** | **59** |

A diferença é de **4 deals (6%)**, não de uma ordem de grandeza. A regra dela continua sendo a
certa — é a definição da dona do processo —, mas **o argumento que usei para defendê-la estava
errado**. Mesma lição de hoje: *schema/forma diz o que PODE acontecer; só a contagem diz o que VAI.*

### Impacto real, se a varredura retroativa rodar

| Transição | Cards que se moveriam **de uma vez** |
|---|---|
| **T3** (qualificado) | **50** — 11 em `Lead novo` + 3 em `Contato Realizado` + 36 em `Em qualificação ` |
| **T1** (24h sem resposta) | **135** dos 242 cards nos estágios iniciais |

⇒ **185 dos 242 cards** dos estágios iniciais mudariam de coluna numa única execução — **77% do
board de trabalho dela**, a 3 dias da apresentação para diretoria + MKT. 🔒 **Confirma o gate de
retroatividade do @po**, agora com número em vez de receio.

✅ **Campos confirmados, nada inventado:** `ondeReside` ("Onde reside") e `tipoDeLesao`
("Tipo de Lesão") existem em `custom_field_definitions`; os valores vivem em `deals.custom_fields`
(jsonb). **AC4b satisfeito.**

🪤 **Armadilha nova para o registro:** a 2.5 documentou espaço **no fim** de `Em qualificação `.
Existe um segundo caso, do outro lado: **` Proposta enviada` tem espaço no INÍCIO**. Casar estágio
por nome falharia nos dois — e um `trim()` ingênuo esconderia o problema em vez de resolvê-lo.
**Só UUID**, como manda o AC1.

## ✂️ Ajuste de escopo @po (Pax) — 2026-08-11, rev. 3

**Decisão do Filipe: caminho (b).** O **T1 sai da 2.17** e passa a ser **número no painel** da
story **2.19** (Bloco A — *"passou de 24h sem minha resposta"*).

**Racional:** o T1 nunca foi uma coluna. A resposta dela (*"24 horas"*) descreve uma **condição de
urgência**, não um estágio do funil — estágio é onde o negócio *está*, não há quanto tempo ninguém
o toca. Como número de painel ele aparece **sem mover card nenhum**: nada se desloca, nada fica
preso pelo AC2, e ela vê os **135** imediatamente.

### O que isto simplifica — e não é pouco

🔑 **Sem T1, a story deixa de precisar de job periódico.** O `pg_cron` estava ali **só** para o T1,
porque *"ausência de evento não dispara webhook"*. T2 e T3 são **disparados por evento** (mensagem
inbound · fim da extração). ⇒ Caem juntos:

- o job periódico e o `pg_cron`;
- o **risco 5 (concorrência job × webhook)** — some a metade "job" da corrida;
- a **estimativa: de G para M**.

### AC revisados

- **AC4** — ❌ **removido desta story** (era o T1 por tempo). Migra para a 2.19.
- **AC4b** — ✅ mantido: *qualificado* = `ondeReside` **E** `tipoDeLesao`.
- **AC7** — casos (a) *[lead sem inbound vai para T1]* **removido**; permanecem (b), (c), (d), (e).
- **AC10 (novo)** — ⛔ **a varredura retroativa nasce DESLIGADA.** A regra vale para evento novo.
  Ligá-la no histórico é ato explícito, com decisão registrada — 185 de 242 cards (77%) se moveriam
  de uma vez, a 3 dias da apresentação.

### 📊 Medição que confirma o valor do T2 (@dev, 11/08)

Havia a hipótese de o T2 ser redundante com a story **2.5** (que já move para `Em qualificação `
quando a IA transfere para humano). **Medido — não é:**

| Estágio | Já responderam e não estão qualificados | Total |
|---|---|---|
| `Lead novo` | **124** | 141 |
| `Contato Realizado` | **46** | 51 |

⇒ **170 cards** respondendo e parados em estágio de entrada. 🔑 **`Lead novo` está 88% mentindo:
124 dos 141 já conversaram.** A regra da 2.5 só dispara **na transferência para humano** — e a maior
parte dos leads responde à IA **sem nunca ser transferida**. O T2 cobre exatamente esse vão.

**Veredito: 🟢 GO para @dev** — T2 + T3, varredura desligada.

## Dev Notes

- Ponto de extensão natural: `lead_routing_rules` (já tem `stage_id` e `transfer_stage_id`).
- `pg_cron` já foi usado no repo (cards em tempo real, 22/07).
- `docs/stories/` é gitignored (pendência nº 9).

## File List

**Novos**
- `lib/deals/autoStage.ts` — decisão pura do T3 (Node)
- `lib/deals/autoStage.test.ts` — 18 testes
- `lib/deals/moveOnQualified.ts` — I/O do T3 + atividade (AC5)
- `test/stageMoveOnReply.test.ts` — 31 testes (T2 + contrato entre os gêmeos)
- `supabase/migrations/20260811210000_add_auto_stages_to_lead_routing_rules.sql`

**Alterados**
- `supabase/functions/messaging-webhook-gptmaker/stage-move.ts` — `decideReplyStageMove` + `isQualified`
- `supabase/functions/messaging-webhook-gptmaker/index.ts` — `moveDealOnReply`, hook no `handleMessage`, `replied_stage_id` na regra, `description` parametrizável na atividade
- `lib/ai/extraction/customFields.service.ts` — chama o T3 após gravar

---

## ✅ Gate @qa (Quinn) — 2026-08-11

| Verificação | Resultado |
|---|---|
| lint | ✅ 0 warnings |
| typecheck | ✅ |
| Suíte | ✅ **651 testes** (eram 620 — **+31**) |
| build | ✅ *(1 warning pré-existente em `next.config.ts`, tracing do Turbopack — sem relação)* |
| AC1 destino por UUID | ✅ nas duas transições |
| AC2 não regride | ✅ provado por mutação nos **dois** runtimes |
| AC3 movimento manual | 🟡 **parcial — ver ressalva** |
| AC5 auditável | ✅ atividade `STATUS_CHANGE` com autoria explícita, em ambos |
| AC6 escrita conferida | ✅ `.select()` + 0 linhas tratado como erro nos dois |
| AC7 testes discriminam | ✅ **provado por mutação**, 2 reprovaram de cada lado |
| AC10 nasce desligada | ✅ colunas `NULL` por padrão + guarda de saída antecipada |
| AC8 provado em uso real | ⏳ **aberto** — depende de configurar e de lead real |

### 🚨 ACHADO DO GATE — a ordem do deploy importa, e errá-la quebra o que já funciona

O webhook passou a ler `replied_stage_id` no `select` da `lead_routing_rules`. **Essa coluna ainda
não existe em produção** — a migração está escrita e **não foi aplicada**.

Se o deploy subir **antes** da migração, o PostgREST devolve erro de coluna desconhecida, o
`getTransferRoutingRule` cai no `return null`… e **`moveDealOnTransfer` para de mover o card**.
Ou seja: **a automação da story 2.5, que está no ar desde 03/08, morre em silêncio** — sem exceção,
sem alarme, com o webhook respondendo 200 e o card parado. É a mesma família do webhook mudo do
Lead Ads: *o sistema não mente, ele silencia.*

⛔ **Ordem obrigatória: (1) aplicar a migração → (2) deploy.** Nunca o inverso.

### ⚠️ Ressalva do AC3 — declarada, não escondida

O AC3 pede que movimento manual da Fernanda tenha a palavra final. O que existe é a
**monotonicidade**: a automação nunca puxa o card para trás, então tudo que ela **avançou** está
protegido. O que **não** está: se ela mover um card **para trás** de propósito, a automação pode
empurrá-lo de novo para a frente.

Não há hoje registro de "quem moveu por último" no schema — implementar isso seria inventar
mecanismo novo dentro de uma story de outro escopo. **A story 2.5 já vive com o mesmo limite desde
03/08.** Fica declarado aqui em vez de marcado como ✅ indevidamente.

**Veredito: 🟢 PASS com 2 condições** — migração antes do deploy, e AC8 fechado depois, em uso real.

---

## ✅ Validação @po (Pax) — 2026-08-07

| # | Ponto | Nota |
|---|---|---|
| 1 | Título claro | ✅ |
| 2 | Descrição completa | ✅ — ataca a "classificação manual" que ela descreveu duas vezes |
| 3 | AC testáveis | ✅ — AC7 lista 5 casos; AC8 exige lead real |
| 4 | Escopo IN/OUT | ✅ — estrelas e desfecho comercial explicitamente fora |
| 5 | Dependências | ✅ — estende `lead_routing_rules` (2.5); interage com a 2.16 |
| 6 | Estimativa | ✅ G |
| 7 | Valor de negócio | ✅ — o maior da leva: 846 leads/mês classificados na mão |
| 8 | Riscos | ✅ — 6, com concorrência e retroatividade |
| 9 | Definição de pronto | ✅ |
| 10 | Alinhamento | ✅ |

**Nota: 10/10 · Veredito: 🟡 GO CONDICIONAL**

**Bloqueio:** duas definições que **só a Fernanda pode dar** — (a) quantas horas sem resposta caracterizam "sem resposta"; (b) **quais campos definem "qualificado"**. O item (b) era o ponto 5 da pauta de 07/08 e ficou **parcial**.
➡️ **O AC0 (medição) pode e deve ser executado antes** — não depende dela e pode derrubar a premissa, como aconteceu na 2.14.

---

## ✅ Revalidação @po (Pax) — 2026-08-11

**Motivo:** os dois bloqueios do GO condicional foram respondidos pela Fernanda por WhatsApp em 11/08.

| Bloqueio de 07/08 | Estado |
|---|---|
| (a) Horas para *"sem resposta"* | ✅ **24 horas** — fecha o AC4 |
| (b) Quais campos definem *"qualificado"* | ✅ **onde mora + tipo de lesão** — fecha o T3, vira AC4b |

### 🔑 O que a resposta dela mudou na story — e não era detalhe

A definição de *"qualificado"* que ela deu é **mais estreita que a extração**: a IA produz **5**
campos, ela usa **2**. A story original dizia *"os campos de qualificação estão preenchidos"* — texto
que, lido pelo @dev, viraria naturalmente **"todos os campos"**. Isso teria feito o T3 disparar
**muito menos** do que ela espera, e o sintoma seria o pior possível: **a coluna "qualificado" quase
vazia**, sem erro nenhum, com todos os gates verdes. ⇒ *A pergunta que parecia burocrática era a
diferença entre a funcionalidade existir e parecer quebrada.*

### 🔴 Um bloqueio NOVO, que não existia em 07/08

A terceira pergunta (**rodar no histórico ou só daqui pra frente?**) estava marcada 🟡 e **sobe para
🔴** — não porque mudou, mas porque **a data ao redor dela mudou**:

1. Ela confirmou em 11/08 que a apresentação de **14/08 é para diretoria + MKT** — ou seja, o board
   vai ser projetado para **quem decide orçamento**, não para uso interno;
2. Ela **migrou de vez** para o CRM em 07/08 e está aprendendo a tela **esta semana**;
3. A 2.24 acabou de mudar o board **hoje** (724 → 293 cards), e a 2.27 mudou o comportamento de
   gravação **três vezes no mesmo dia**.

⇒ Uma varredura retroativa **reorganizaria os 293 cards de uma vez**, na véspera da apresentação,
para uma usuária que está há 4 dias na ferramenta. **Não é decisão técnica — é decisão do dono.**

### Veredito

**🟢 GO para o AC0 e para a implementação** — nada mais depende da Fernanda.
**⛔ GATE antes do deploy:** a decisão de retroatividade tem de estar tomada e registrada antes de
qualquer `UPDATE` de estágio chegar à produção. O @dev implementa **com a varredura desligada por
padrão**; ligá-la é ato explícito.

➡️ **Próximo passo: @dev executa o AC0** — que continua podendo derrubar a premissa, como na 2.14.
